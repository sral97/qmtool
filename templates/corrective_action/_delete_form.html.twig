<form method="post" action="{{ path('corrective_action_delete', {'id': corrective_action.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ corrective_action.id) }}">
    <button class="btn">Delete</button>
</form>
