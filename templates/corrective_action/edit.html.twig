{% extends 'base.html.twig' %}

{% block title %}Edit CorrectiveAction{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <a href="{{ path('index') }}">Index</a>
                &gt; <a href="{{ path('incident_index') }}">{{ 'Incidents'|trans }}</a>
                &gt; <a href="{{ path('incident_index') }}">{{ 'Incident'|trans }} {{ corrective_action.incident.id }}</a>
                &gt; {{ 'Edit Corrective Action'|trans }}

                <h1>F 2.3.1.02 {{ 'Edit Corrective Action'|trans }}</h1>

                {{ include('corrective_action/_form.html.twig', {'button_label': 'Update'}) }}

{#                {{ include('corrective_action/_delete_form.html.twig') }}#}
            </div>
        </div>
    </div>

{% endblock %}
