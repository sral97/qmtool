{% extends 'base.html.twig' %}

{% block title %}Edit Incident{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <a href="{{ path('index') }}">Index</a> &gt; <a href="{{ path('incident_index') }}">{{ 'Incidents'|trans }}</a> &gt; {{ 'Edit Incident'|trans }}

                <h1>Edit Incident</h1>

                {{ include('incident/_form.html.twig', {'button_label': 'Update'}) }}

{#                {{ include('incident/_delete_form.html.twig') }}#}
            </div>
        </div>
    </div>
{% endblock %}
