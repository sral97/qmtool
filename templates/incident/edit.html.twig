{% extends 'base.html.twig' %}

{% block title %}Edit Incident{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <a href="{{ path('index') }}">Index</a>
                &gt; <a href="{{ path('incident_index') }}">{{ 'Incidents'|trans }}</a>
                &gt; <a href="{{ path('incident_show', {'id': incident.id}) }}">{{ 'Incident'|trans }} {{ incident.id }}</a>
                &gt; {{ 'Edit Incident'|trans }}

                <h1>F 2.3.1.01 {{ 'Edit Incident %incidentId%'|trans({'%incidentId%': incident.id}, 'messages+intl-icu') }}</h1>

                {{ include('incident/_form.html.twig', {'button_label': 'Update'}) }}

{#                {{ include('incident/_delete_form.html.twig') }}#}
            </div>
        </div>
    </div>
{% endblock %}
